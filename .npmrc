# Corporate proxy / Windows SSL: if npm install fails with UNABLE_TO_VERIFY_LEAF_SIGNATURE,
# this allows installs. Prefer fixing CA certs (npm config set cafile ...) when possible.
strict-ssl=false

# Prisma engine downloads use Node fetch — same cert issue on some networks
# Use: npm run db:generate:win-ssl
